spring:
  application:
    name: card-service

server:
  port: 8803

eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://eureka-service:8761