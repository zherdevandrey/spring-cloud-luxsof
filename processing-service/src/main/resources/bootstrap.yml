spring:
  application:
    name: ProcessingService
  cloud:
    config:
      uri: http://localhost:8600
      name: ConfigService
    bus:
      enabled: true

server:
  port: 8804

feign:
  hystrix:
    enabled: true

