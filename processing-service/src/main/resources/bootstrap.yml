spring:
  application:
    name: ProcessingService
  cloud:
    config:
      uri: http://localhost:8600
      name: ConfigService

server:
  port: 8804

feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream