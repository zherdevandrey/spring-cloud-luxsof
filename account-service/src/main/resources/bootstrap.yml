spring:
  application:
    name: AccountService
  cloud:
    config:
      uri: http://localhost:8600
      name: ConfigService
    bus:
      enabled: true
  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
  sleuth:
    sampler:
      probability: 1
server:
  port: 8802

logging:
  file:
    name: /home/user/IdeaProjects/spring-cloud-luxsoft/log/account.log